class ApiHelper{

  // // Cache keys should be unique per API endpoint
  // static String _getTotalPagesKey(String apiEndpoint) => '${apiEndpoint}_total_pages';
  //
  // // Store total pages for a specific API endpoint
  // static void storeTotalPages(String apiEndpoint, int totalPages) {
  //   final key = _getTotalPagesKey(apiEndpoint);
  //   _cacheBox.put(key, totalPages);
  // }
  //
  // // Read total pages for a specific API endpoint
  // static int? readTotalPages(String apiEndpoint) {
  //   final key = _getTotalPagesKey(apiEndpoint);
  //   return _cacheBox.get(key);
  // }
  //
  // // Clear total pages cache for a specific API endpoint
  // static void clearTotalPages(String apiEndpoint) {
  //   final key = _getTotalPagesKey(apiEndpoint);
  //   _cacheBox.delete(key);
  // }

}